<odoo>
  <data>
    <!-- explicit list view definition -->
    
    <record model="ir.ui.view" id="asignar_wizard">

      <field name="name">Asignar Wizard</field>
      <field name="model">asignar.asignar.wizard</field> 
      <field name="arch" type="xml">
      <form string="Asignando">
      <p>Asigne vendedores a clientes</p>
      </form>
      </field>
    </record>

    <!-- <record model="ir.actions.act_window" id="asignar_w.action_window">
      <field name="name">Asignaciones</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">asignar.asignar.wizard</field>
      <field name="view_type">tree</field>
      <field name="view_mode">tree</field>
      <field name="target">new</field>
    </record>
   
<menuitem
  id="asignacion_menu"
  name="Asignar"
  action="asignar_w.action_window"
  parent="asignar.menu_root"/> -->

  </data>
</odoo>